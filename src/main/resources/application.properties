spring.application.name=portfolio-api
# server.servlet.context-path=/portfolioApi (Removed for simpler root access)

# Database (Neon)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-lively-rain-a1euflno-pooler.ap-southeast-1.aws.neon.tech:5432/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_i9vL3gxOfnpw}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Optional (better debugging)
spring.jpa.properties.hibernate.format_sql=true

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Credentials are now stored in the database (SystemConfig table)
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true